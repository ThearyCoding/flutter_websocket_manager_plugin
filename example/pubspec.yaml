name: websocket_manager_example
description: Demonstrates how to use the websocket_manager plugin.
publish_to: 'none'
version: 1.0.0+1


environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  websocket_manager:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
